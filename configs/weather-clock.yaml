# ESP32-S3-Touch-LCD-7
substitutions:
  name: the
  friendly_name: "${name} weather clock"
  dname: ${name}-weatherclock
  uname: ${name}_weatherclock
  tempscale: "°F"

# Wifi Setup
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_ssid_password

# Packages
packages:
  setup:
    url: https://github.com/pkscout/ESPHome/
    files: [configs/weather-clock/device/esp32-s3-touch-lcd-7.yaml,
            configs/weather-clock/addon/backlight.yaml,
            configs/weather-clock/addon/network.yaml,
            configs/weather-clock/assets/fonts.yaml,
            configs/weather-clock/assets/icons.yaml,
            configs/weather-clock/device/sensors.yaml,
            configs/weather-clock/device/lvgl.yaml]
    refresh: 1sec
